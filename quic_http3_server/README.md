# QUIC HTTP/3 Server

This directory contains a simple Python-based HTTP/3 server using the `aioquic` library.
It's intended for testing QUIC implementations, such as a QUIC-enabled curl.

## TODO

- Detailed setup instructions
- Server code
- Certificate generation steps
- Curl testing examples
