// Placeholder for trikeshed-core Kotlin/JS type definitions: trikeshedCore.d.ts
// This file would normally be generated by the Kotlin/JS compiler.

// These are highly conceptual and simplified. Actual .d.ts would be more complex.
declare module 'trikeshedCore' { // Assuming it's accessible as a module
    export interface Join<A, B> {
        readonly a: A;
        readonly b: B;
    }
    export function j<A, B>(a: A, b: B): Join<A, B>;

    export interface Series<T> {
        readonly size: number;
        get(index: number): T;
        // Placeholder for `.`▶` which would return an Iterable
        asIterable(): Iterable<T>;
    }
    export function emptySeries<T>(): Series<T>;

    export interface Tensor<T> {
       readonly shape: number[];
       // Placeholder, actual accessor might be different
       get(coords: number[]): T;
    }
    export function TensorConstruct<T>(shape: number[], accessor: (coords: number[]) => T): Tensor<T>;
    export function TensorSeries<T>(size: number, accessor: (index: number) => T): Tensor<T>;
    export function TensorCursor<T>(rows: number, cols: number, accessor: (row: number, col: number) => T): Tensor<T>;


    export enum IOMemento {
        IoByte, IoShort, IoInt, IoFloat, IoDouble, IoLong,
        IoBoolean, IoChar, IoString, IoCharSeries, IoBigDecimal,
        IoBigInt, IoDateTime, IoDuration, IoUUID, IoBinary,
        IoUnknown
    }
    // ... (other type definitions for ColumnMeta, CursorMeta, TypeMemento, ColumnExclusion etc.)
    // For example:
    export interface TypeMemento {
        // properties based on actual Kotlin interface
    }
    export type ColumnMeta<N, T extends TypeMemento> = Join<N, T>; // Assuming N is typically String
    // export type CursorMeta<CM> = Tensor<CM>; // CM would be ColumnMeta

    export class ColumnExclusion {
        constructor(name: string);
        readonly name: string;
    }
    export function unaryMinus(value: string): ColumnExclusion; // Conceptual for String.unaryMinus()
}
